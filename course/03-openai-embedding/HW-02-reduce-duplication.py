"""
===============================================================================
作业 02：减少重复代码 - 函数封装实践
===============================================================================

问题背景
-------------------------------------------------------------------------------

在 text_embedding.py 中，我们使用了完全面向过程的编程风格，这对理解每一步的
执行过程很有帮助。但观察代码，会发现有大量重复：

    # 获取文本1的向量
    print(f"\n正在向量化文本1: {text1}")
    response1 = client.embeddings.create(model=EMBEDDING_MODEL_NAME, input=text1)
    embedding1 = np.array(response1.data[0].embedding)
    print(f"向量维度: {embedding1.shape}")
    print(f"向量前10个值: {embedding1[:10]}")
    
    # 获取文本2的向量
    print(f"\n正在向量化文本2: {text2}")
    response2 = client.embeddings.create(model=EMBEDDING_MODEL_NAME, input=text2)
    embedding2 = np.array(response2.data[0].embedding)
    print(f"向量维度: {embedding2.shape}")
    print(f"向量前10个值: {embedding2[:10]}")
    
    # 获取文本3的向量
    print(f"\n正在向量化文本3: {text3}")
    response3 = client.embeddings.create(model=EMBEDDING_MODEL_NAME, input=text3)
    embedding3 = np.array(response3.data[0].embedding)
    print(f"向量维度: {embedding3.shape}")
    print(f"向量前10个值: {embedding3[:10]}")

这三段代码几乎完全一样，只是变量名不同。如果要处理 10 个文本，就需要复制
10 次这段代码，这会导致：

1. 代码冗长，可读性下降
2. 修改困难：如果需要调整输出格式，要改 10 处
3. 容易出错：复制粘贴时可能忘记修改某些变量名

作业目标
-------------------------------------------------------------------------------

学习如何通过函数封装来减少重复代码，理解"抽象"和"复用"的编程思想。

任务要求
-------------------------------------------------------------------------------

重构 text_embedding.py，通过定义函数来消除重复代码，同时保持代码的可读性。


扩展思考
-------------------------------------------------------------------------------

1. 如果不使用函数，还有其他方式减少重复吗？
   - 使用循环处理多个文本
   - 各有什么优缺点？

2. 如何在"可读性"和"简洁性"之间平衡？
   - 过度封装可能导致代码难以理解
   - 不封装导致代码冗长
   - 如何找到平衡点？

提交要求
-------------------------------------------------------------------------------

1. 重构后的代码应该：
   - 功能与 text_embedding.py 完全相同
   - 代码行数明显减少
   - 可读性保持或提升

2. 在代码开头注释中说明：
   - 你定义了哪些函数
   - 为什么这样设计
   - 重构前后的代码行数对比

3. 运行测试：
   python HW-02-reduce-duplication.py
   
   输出应该与 text_embedding.py 基本一致

===============================================================================
"""

# 你的代码从这里开始

"""
===============================================================================
作业 01：使用环境变量管理 API 配置
===============================================================================

问题背景
-------------------------------------------------------------------------------

在之前的程序中，我们直接在代码中写入了 API_KEY：

    BASE_URL = "http://143.64.120.39:8695/v1"
    API_KEY = "sk-7a1xxxxxxxxxxxxxx"
    EMBEDDING_MODEL_NAME = "NNIT-Ada-3-large"

这种做法存在两个严重问题：

1. 繁琐：每次换环境或分享代码，都需要手动修改这些值
2. 高危：如果不小心将代码提交到 GitHub 等公开平台，API_KEY 就会泄露
   - 他人可以使用你的 API_KEY 消耗你的额度
   - 可能产生高额费用
   - 存在安全风险

作业目标
-------------------------------------------------------------------------------

学习使用环境变量和配置文件来管理敏感信息，实现以下目标：

1. 创建 .env 文件存储真实的配置信息（不提交到 Git）
2. 创建 .env.example 文件作为配置模板（可以提交到 Git）
3. 使用 python-dotenv 包读取环境变量
4. 修改代码，从环境变量获取配置而不是硬编码

任务步骤
-------------------------------------------------------------------------------

步骤 1：安装 python-dotenv 包
    pip install python-dotenv

步骤 2：在项目根目录参照 .env.example 文件创建 .env 文件（注意：这个文件应该被 .gitignore 忽略）
    文件内容示例：
    BASE_URL=http://143.64.120.39:8695/v1
    API_KEY = "sk-7a1xxxxxxxxxxxxxx"
    LLM_MODEL_NAME=gpt-5-chat
    EMBEDDING_MODEL_NAME=NNIT-Ada-3-large

步骤 3：修改代码，使用环境变量传入API_KEY

步骤 4：确保 .gitignore 文件包含 .env
    检查项目根目录的 .gitignore 文件是否包含：
    .env
    如果没有，需要添加这一行

提交要求
-------------------------------------------------------------------------------

完成后，你的项目目录应该包含：
- HW-01-env-config.py（本文件，修改后的代码）
- .env（本地使用，不提交到 Git）
- .env.example（配置模板，提交到 Git）
- .gitignore（确保包含 .env）

运行测试：
    python HW-01-env-config.py

如果配置正确，应该能看到与 text_embedding.py 相同的输出。

===============================================================================
"""

# 你的代码从这里开始
